#include <stdio.h>
/**
 * main - A programmer that print exactly with proper grammar, but the outcome is a piece of art,
 * Return: o (sucessful)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art, \n");
	return (0);
}
